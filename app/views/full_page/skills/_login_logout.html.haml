%p
  #login_out
    = current_user.inspect if user_signed_in?
    = link_to "Logout", destroy_user_session_path, method: :delete, remote: true if user_signed_in?
    = link_to "Login", new_user_session_path unless user_signed_in?